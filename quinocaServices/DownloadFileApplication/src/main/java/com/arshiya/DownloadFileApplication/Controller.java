package com.arshiya.DownloadFileApplication;

import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.http.ResponseEntity;
import org.springframework.web.bind.annotation.GetMapping;
import org.springframework.web.bind.annotation.RestController;
import org.springframework.web.client.RestTemplate;

//@RestController
public class Controller {

   // @Autowired
  // private RestTemplate restTemplate;

   // @GetMapping("/usersFromOtherService")
    //public ResponseEntity<String> getUsers() throws InterruptedException {
      //  Thread.sleep(1000);
      //  return restTemplate.getForEntity("http://localhost:8081/usersList", String.class );
   // }

}
